// examples/demo.ltl — runnable demo for ltl_rogue_lite

import std::io
use ltl_rogue_lite as lib

fn main() {
    io::println("ltl_rogue_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}