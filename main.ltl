// ltl-rogue-lite/main.ltl
//
// ASCII roguelike with FOV + A* + procedural dungeons.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-rogue-lite v0.1.0")
    io::println("ASCII roguelike with FOV + A* + procedural dungeons.")
    0
}
